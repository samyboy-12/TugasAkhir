<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tugasakhir">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Add other necessary permissions if required -->
    <!-- Example: <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TugasAkhir"
        tools:targetApi="31">

        <!-- MainActivity Declaration -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HomeActivity Declaration -->
        <activity android:name=".HomeActivity" />

        <!-- LoginActivity Declaration -->
        <activity android:name=".LoginActivity" />

        <!-- FavoriteActivity Declaration -->
        <activity android:name=".FavoriteActivity" />

        <!-- OverviewActivity Declaration -->
        <activity android:name=".OverviewActivity" />

        <activity android:name=".RegisterActivity" />

        <activity android:name=".ReadActivity" />

        <!-- Add any additional activities here as required -->
        <!-- Example: -->
        <!-- <activity android:name=".ReadActivity" /> -->

    </application>
</manifest>
